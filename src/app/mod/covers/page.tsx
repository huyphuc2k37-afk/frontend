import type { Metadata } from "next";
export const metadata: Metadata = { title: "Duyệt ảnh bìa – Kiểm duyệt | VStory" };
import CoversPage from "./CoversPage";
export default function Page() { return <CoversPage />; }

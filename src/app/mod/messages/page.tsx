import type { Metadata } from "next";
export const metadata: Metadata = { title: "Nhắn tin – Kiểm duyệt | VStory" };
import MessagesPage from "./MessagesPage";
export default function Page() { return <MessagesPage />; }
